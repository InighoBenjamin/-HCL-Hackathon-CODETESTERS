# ================================================================
#   config.properties — CODETESTERS Central Configuration
#
#   ⚡ HACKATHON DAY INSTRUCTIONS:
#   ONLY change the values below — never touch the Java files!
#   Step 1: Replace app.url with the URL given at 9:00 AM
#   Step 2: Replace valid.username / valid.password with real creds
#   Step 3: Save → Run tests ✅
# ================================================================

# --- 🌐 Application URL (CHANGE THIS on hackathon day) ---
app.url=https://www.example.com

# --- 🔐 Login Credentials (CHANGE THESE on hackathon day) ---
valid.username=admin
valid.password=admin123
invalid.username=wronguser@test.com
invalid.password=wrongpass999

# --- 🌍 Browser ---
browser=chrome

# --- ⏱ Timeouts (seconds) ---
implicit.wait=10
explicit.wait=20
page.load.timeout=30

# --- 📊 Excel Test Data ---
testdata.file.path=src/test/resources/testdata/TestData.xlsx
testdata.sheet.login=LoginData
testdata.sheet.workflow=WorkflowData
